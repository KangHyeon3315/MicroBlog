spring:
  config:
    import: ""

  cloud:
    config:
      uri: ""
      fail-fast: true
      retry:
        initial-interval: 3000
        multiplier: 1.3
        max-interval: 10000
        max-attempts: 20
      profile: ""